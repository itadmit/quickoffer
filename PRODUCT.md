# QuickVoice — אפיון מוצר

**Repo:** https://github.com/itadmit/quickvoice

**גרסה:** 0.4 (טיוטה) · **תאריך:** 20.9.2026 · **סטטוס:** לאישור

> מסמך זה מחליף את [start.md](start.md) כמקור האמת למוצר. start.md נשאר כתיעוד של הרעיון המקורי.

---

## 1. תמצית

**משפט אחד:** בעל מקצוע שולח הודעה קולית ב-WhatsApp — ומקבל בחזרה הצעת מחיר מעוצבת, מוכנה להעברה ללקוח, תוך דקה.

**המסר השיווקי:** שלח הודעה קולית. קבל הצעת מחיר. סגור עסקה.

**הכאב:** היום ההצעה נשלחת כהודעת WhatsApp חופשית ("3 נקודות 450 + ביקור 200") או לא נשלחת בכלל. זה לא נראה מקצועי, אין מעקב אחרי מה נשלח ומה אושר, וקשה לסגור.

**למה זה עובד:** אפס התקנה, אפס הרשמה, אפס למידה. ההודעה הקולית היא כבר ההרגל של הקהל הזה. הממשק הוא WhatsApp; web רק איפה שחייבים טופס.

---

## 2. פרסונות

### בעל המקצוע (המשתמש המשלם)
- עצמאי בתחום שטח: חשמל, אינסטלציה, מיזוג, שיפוצים, אלומיניום, גינון, התקנות.
- נמצא ברכב או באתר. ידיים עסוקות. לא ליד מחשב. WhatsApp פתוח כל היום.
- שולח 5–30 הצעות בחודש, רובן 1–5 סעיפים, בין 300 ל-15,000 ₪.
- רגישות גבוהה למחיר חודשי; מוכן לשלם אם זה חוסך זמן או סוגר יותר עסקאות.
- **קהל הרחבה (שלב 2):** צלמים, מעצבים, סוכנויות, נותני שירות.

### הלקוח הסופי (לא נרשם, לא מתקין)
- בעל בית פרטי שקיבל קישור ב-WhatsApp מבעל המקצוע.
- פותח מהטלפון. רוצה להבין מה כלול, כמה זה עולה, ולאשר בלחיצה.
- דורש: עמוד מהיר, בעברית, RTL, ברור. בלי הרשמה.

---

## 3. עקרונות מוצר

1. **WhatsApp הוא הממשק.** כל מה שאפשר לעשות בצ׳אט — עושים בצ׳אט. Web רק לעריכה מפורטת ולדף הלקוח.
2. **קול קודם.** כל אינטראקציה שאפשר לעשות בדיבור (יצירה, תיקון) נתמכת בקול. טקסט הוא fallback.
3. **דקה אחת.** מהודעה קולית להודעה מוכנה להעברה — פחות מ-60 שניות, כולל תמלול.
4. **לא הנהלת חשבונות.** לא חשבוניות, לא קבלות, לא CRM. הצעות מחיר בלבד; חיבור לתוכנות הנה"ח בשלב 2.
5. **ניתן להחלפה.** שכבת WhatsApp (iBot) מאחורי ממשק אחד (`sendText`, `sendDoc`, `onMessage`) — מעבר ל-Cloud API בעתיד הוא יום עבודה.

---

## 4. ארכיטקטורה

```
                       ┌──────────────────────┐
  בעל מקצוע ──WhatsApp──▶│   iBot (instance)    │
             ◀──────────│  webhook ▶ / send-* ◀ │
                       └──────────┬───────────┘
                                  │ POST (כל הודעה נכנסת)
                                  ▼
                       ┌──────────────────────┐        ┌─────────────┐
                       │  QuickVoice server   │───────▶│ תמלול (ASR) │
                       │  Next.js + Postgres  │◀───────│ LLM → JSON  │
                       │  תור עבודות          │        └─────────────┘
                       └──────┬─────────┬─────┘
                              │         │
                    magic link│         │public link
                              ▼         ▼
                       ┌──────────┐ ┌──────────┐
                       │ מסך עריכה│ │ דף לקוח  │◀── לקוח (דפדפן, מובייל)
                       └──────────┘ └──────────┘
```

**רכיבים:**
- **iBot** — מספר WhatsApp ייעודי של QuickVoice. קלט דרך webhook, פלט דרך `send-text` / `send-doc`.
- **QuickVoice server** — Next.js 16 (App Router), Postgres (Neon), תור עבודות לעיבוד אסינכרוני.
- **AI** — תמלול עברית + LLM להמרה ל-JSON מובנה ולתיקונים.
- **Storage** — R2/Cloudinary ללוגואים, הקלטות (לצורכי דיבוג, 30 יום), חתימות, PDF.
- **Web** — שני מסכים ציבוריים (עריכה עם טוקן חתום; דף לקוח), ללא מערכת login.

---

## 5. חוזה iBot

### 5.1 Webhook נכנס

iBot שולח `POST` לכתובת מוגדרת עם כל הודעה נכנסת, עם header `X-Webhook-Token`. פיילוד (אחרי התיקון ב-iBot, 20.9.2026):

```json
{
  "uid": "…",
  "sessionId": "…",
  "instanceId": "…",
  "chatId": "…",
  "remoteJid": "972542284283@s.whatsapp.net",
  "msgFromMe": false,
  "actualObj": {
    "group": false,
    "type": "text" | "audio" | "image" | "document" | …,
    "msgId": "2A53BDA9FDE380D45EE7",
    "senderName": "יוגב אביטן תדמית אינטראקטיב",
    "timestamp": 1789896969,
    "route": "incoming",
    "msgContext": {
      "text": "...",                                            // type=text
      "fileName": "ioIgM6_inbox.oga",                           // מדיה
      "mimetype": "audio/ogg; codecs=opus",
      "mediaUrl": "https://ibot-chat.com/media/ioIgM6_inbox.oga",
      "caption": "", "width": 1152, "height": 2048              // image
    },
    "context": null | { "jid": "…", "id": "…" }                 // הודעה מצוטטת
  }
}
```

הודעות ללא `actualObj` (סוג לא מזוהה) לא מפעילות webhook בכלל.

**נורמליזציה בצד QuickVoice** (`InboundMessage`):

| שדה | מקור | הערה |
|---|---|---|
| `id` | `actualObj.msgId` | מפתח דדופ (unique בטבלת `inbound_messages`) |
| `from` | `remoteJid` | זהות המשתמש. `972542284283@s.whatsapp.net` → `972542284283` |
| `fromName` | `actualObj.senderName` | ברירת מחדל לשם העסק באונבורדינג |
| `type` | `actualObj.type` | `text` / `audio` / `image` / `document` / `other`. לקבל גם `aud` (הערך הישן) ליתר ביטחון |
| `text` | `msgContext.text` | |
| `media` | `msgContext.mediaUrl` + `mimetype` + `fileName` | למשוך מיד, ראה 5.2 |
| `quotedId` | `context?.id` | `null` כשאין ציטוט |
| `at` | `actualObj.timestamp` | Unix seconds; אם חסר — זמן הקבלה |

**סינון — נזרק בשקט (200 בלי עיבוד):**
- `actualObj.group === true`
- `msgFromMe === true`
- `actualObj.route !== "incoming"`
- `msgId` שכבר נראה (webhook כפול)
- `type` לא נתמך (סטיקר, מיקום, איש קשר) → תשובה: "אני מבין הודעות קוליות, טקסט ותמונות (ללוגו)."

**התנהגות ה-handler:**
1. אימות (5.3) → 401 אם נכשל.
2. נורמליזציה + סינון + כתיבה ל-`inbound_messages`.
3. החזרת `200` **מיד** (< 500ms). כל העיבוד — תמלול, LLM, שליחה — בתור עבודות, לא בבקשת ה-webhook.
4. אם ה-webhook נכשל (500) — iBot לא מבטיח retry, ולכן כתיבה ל-DB קודמת לכל דבר אחר.

### 5.2 מדיה

`msgContext.mediaUrl` מצביע לקובץ סטטי ב-`https://ibot-chat.com/media/<fileName>`. שני אילוצים שקובעים את ההתנהגות שלנו:

- **אין אימות על `/media`** — מי שיש לו את ה-URL מוריד. לכן ה-URL **לא נשמר** ב-DB לטווח ארוך (רק ב-`raw` של `inbound_messages`, שנמחק אחרי 7 ימים).
- **iBot מוחק מדיה אחרי 30 יום.** לכן המשיכה היא **הפעולה הראשונה** בעבודה האסינכרונית, מיד אחרי ה-200: הורדה → העלאה ל-R2 (`audio/{quoteId}.oga`, שמירה 30 יום לדיבוג) → תמלול מהעותק שלנו.

הורדה נכשלת (404/timeout) → retry ×3 בתוך דקה, ואז הודעת שגיאה למשתמש (§6.7) + התראה למפעיל.

### 5.3 אימות ה-webhook

- iBot שולח `X-Webhook-Token: <webhook_token>`. הטוקן נוצר בעמוד ה-webhook בדשבורד iBot — **חובה ליצור אותו** לפני חיבור, כי בלעדיו iBot לא שולח header.
- QuickVoice: השוואה ב-constant-time ל-`IBOT_WEBHOOK_TOKEN`; חסר או לא תואם → `401` בלי עיבוד.
- (עתידי, רצוי) HMAC על הגוף: `X-Signature: sha256=<hmac(body, webhook_token)>` — מגן גם על replay ושינוי גוף.
- ה-webhook endpoint גם מגביל קצב (rate limit) לפי IP, ומגביל גודל גוף ל-64KB.

### 5.4 API יוצא

| פעולה | Endpoint | שימוש ב-QuickVoice |
|---|---|---|
| טקסט | `GET /send-text?token&instance_id&jid&msg` | כל הודעות הבוט, קישורים, התראות |
| מסמך | `GET /send-doc?...&docurl&caption` | PDF של ההצעה (לפי בקשה) |
| תמונה | `GET /send-image` | תצוגה מקדימה של ההצעה (שלב 2) |

**כללים:**
- **תור יוצא** — הודעה אחת בכל פעם, ריווח 300–500ms, retry ×3 עם backoff. iBot מבקש להימנע מקריאות מקבילות.
- `msg`/`caption` ב-URL encoding. אורך הודעה עד ~4,000 תווים; לפצל אם צריך.
- `docurl` חייב להיות ציבורי — PDF נשמר ב-R2 עם URL חתום לשעה.
- `token` ו-`instance_id` — רק בשרת, לעולם לא בלקוח.
- לוג של כל שליחה ב-`outbound_messages` (jid, סוג, תוכן, תשובת iBot).

### 5.5 בריאות החיבור

ה-instance של iBot יכול להתנתק (טלפון כבוי, WhatsApp דורש סריקה מחדש). זו נקודת הכשל התפעולית המסוכנת ביותר — הודעות קוליות ייעלמו בשקט.

- נדרש מ-iBot: endpoint סטטוס (`GET /instance-status`) או webhook על ניתוק.
- QuickVoice: בדיקה כל 5 דקות + התראה (SMS/מייל למפעיל) אם מנותק. שליחת `send-text` שנכשלת עם `Instance not connected` → התראה מיידית.

---

## 6. זרימות שיחה (מכונת מצבים)

### 6.1 מצב משתמש

```
[הודעה ראשונה ממספר לא מוכר]
        │
        ▼
   ONBOARDING ──(שם, מע״מ, לוגו)──▶ READY
                                     │
                          ┌──────────┴──────────┐
                          ▼                     ▼
                   [אין טיוטה פעילה]      [טיוטה פעילה]
                   הודעה קולית/טקסט        הודעה קולית/טקסט
                   → הצעה חדשה             → LLM מסווג: תיקון / הצעה חדשה / פקודה
```

**טיוטה פעילה** = ההצעה האחרונה של המשתמש, אם נוצרה ב-30 הדקות האחרונות ועדיין בסטטוס `draft`. תיקונים בצ׳אט מתייחסים אליה. "חדש" או הודעה שמזוהה כהצעה חדשה — פותחת טיוטה חדשה.

### 6.2 אונבורדינג (3 הודעות, בלי טפסים)

```
בוט: היי! אני QuickVoice — הופך הודעות קוליות להצעות מחיר מעוצבות.
     שתי שאלות קצרות ומתחילים.
     1️⃣ איך קוראים לעסק? (לפי WhatsApp: "יוסי חשמל" — שלח "כן" או שם אחר)

משתמש: כן

בוט: 2️⃣ עוסק פטור או עוסק מורשה?
     (מורשה = ההצעות יכללו מע״מ 18%)

משתמש: מורשה

בוט: מעולה. יש לוגו? שלח אותו כתמונה, או "דלג".

משתמש: [תמונה]

בוט: ✅ מוכן! עכשיו פשוט שלח לי הודעה קולית, למשל:
     🎤 "הצעת מחיר לדני כהן — שלוש נקודות חשמל 180 שקל ליחידה, ביקור 200"
     ואני מחזיר הצעה מוכנה תוך דקה.
     פרטים נוספים (כתובת, ח.פ., תנאי תשלום קבועים) — כאן: <קישור הגדרות>
```

- כל תשובה מנותחת ע"י LLM (לא string matching) — "אני מורשה", "פטור ממע״מ", "יוסי החשמלאי" כולם מובנים.
- אפשר לצאת מאונבורדינג באמצע: הודעה קולית שנראית כמו הצעה → מעבדים אותה, וברירות מחדל (מורשה, בלי לוגו) מסומנות להשלמה.

### 6.3 הצעה חדשה מהודעה קולית

```
משתמש: 🎤 "הצעת מחיר לדני כהן, התקנת שלושה גופי תאורה 150 שקל ליחידה,
            ביקור 200 שקל, המחיר לפני מע״מ, 50 אחוז מקדמה"

בוט: ⏳ מעבד...                              ← רק אם התמלול לוקח > 3 שניות

בוט: 📋 הצעה #1042 — דני כהן
     • התקנת גוף תאורה ×3 — 450 ₪
     • ביקור ×1 — 200 ₪
     סה״כ 650 ₪ + מע״מ = 767 ₪
     תשלום: 50% מקדמה, היתרה בסיום

     ✏️ לתקן: כתוב או תגיד לי ("תשנה ביקור ל-250")
     🖊️ עריכה מלאה: https://qv.app/e/x7Kp2…

בוט: 👇 להעביר ללקוח — לחץ לחיצה ארוכה על ההודעה הבאה ← Forward

בוט: שלום דני, מצורפת הצעת מחיר מיוסי חשמל:
     https://qv.app/q/a8Hd3kQ
     ההצעה תקפה ל-14 יום. לאישור — לחץ על הקישור.
```

**החלטות:**
- ההודעה השלישית היא **הודעה נפרדת ונקייה** כדי שה-Forward יעביר בדיוק אותה. הלקוח מקבל אותה **מהמספר של בעל המקצוע** — אמינות.
- הקישור הציבורי מציג תמיד את **הגרסה העדכנית** של ההצעה, עד שהיא מאושרת (ואז מוקפאת). אז גם אם העביר לפני שתיקן — הלקוח רואה נכון.
- PDF **לא** נשלח אוטומטית (יהיה לא מעודכן אחרי תיקון). נשלח לפי בקשה: "PDF" בצ׳אט או כפתור במסך העריכה.
- אם חסר שם לקוח: הבוט יוצר את ההצעה ושואל "למי ההצעה?" — לא חוסם.
- אם חסר מחיר לפריט: הפריט נוצר עם מחיר 0 ומסומן ⚠️ בסיכום ובמסך העריכה. (שלב 2: השלמה מקטלוג אישי.)

### 6.4 תיקונים בצ׳אט

```
משתמש: תשנה את הביקור ל-250 ותוסיף שקע כפול 120

בוט: ✅ עודכן:
     • ביקור — 250 ₪ (היה 200)
     • + שקע כפול ×1 — 120 ₪
     סה״כ 820 ₪ + מע״מ = 967.60 ₪
```

- קלט: ההצעה הנוכחית (JSON) + ההוראה (טקסט או תמלול). פלט: JSON חדש + רשימת שינויים לתצוגה.
- LLM מסווג כל הודעה כשיש טיוטה פעילה: `correction` / `new_quote` / `command` / `question`. ספק → שואל: "לתקן את ההצעה לדני, או הצעה חדשה?"

### 6.5 פקודות טקסט

| הודעה | פעולה |
|---|---|
| `הצעות` | 5 ההצעות האחרונות עם סטטוס + קישורי עריכה |
| `שלח` / `שלחתי` | מסמן את הטיוטה הפעילה כ-`sent` |
| `PDF` | שולח PDF של הטיוטה הפעילה / האחרונה |
| `בטל` | מוחק את הטיוטה הפעילה |
| `חדש` | סוגר הקשר של טיוטה פעילה |
| `הגדרות` | קישור לדף הגדרות העסק |
| `עזרה` | תפריט קצר |

מזוהות ע"י LLM עם fallback ל-exact match — "תבטל", "מחק את זה", "תשלח לי PDF" עובדים.

### 6.6 אירועי לקוח → התראות לבעל המקצוע

| אירוע | הודעה לבעל המקצוע | תזמון |
|---|---|---|
| צפייה ראשונה | 👀 דני כהן פתח את ההצעה #1042 | מיידי, פעם אחת |
| אישור + חתימה | ✅ דני כהן אישר וחתם על הצעה #1042 (767 ₪). PDF חתום מצורף. | מיידי + `send-doc` |
| דחייה | ❌ דני כהן דחה את הצעה #1042. סיבה: "יקר מדי" | מיידי |
| שאלה | 💬 דני כהן שאל על #1042: "זה כולל חומרים?" — ענה לו ישירות ב-WhatsApp | מיידי |
| לא נצפתה | ⏰ ההצעה לדני כהן לא נפתחה כבר 3 ימים. לשלוח תזכורת? | שלב 2 |
| פג תוקף | ⌛ הצעה #1042 לדני כהן פגה (14 יום). "הארך" להאריך ב-14 יום | שלב 2 |

### 6.7 שגיאות

| מצב | תגובה |
|---|---|
| תמלול נכשל / ריק | "לא הצלחתי לשמוע 🙉 נסה שוב במקום שקט יותר, או כתוב לי את ההצעה בטקסט." |
| תמלול הצליח, LLM לא מצא פריטים | "שמעתי: '…' — אבל לא זיהיתי פריטים ומחירים. נסה: 'לדני — 3 נקודות חשמל 180 שקל ליחידה'." |
| מדיה לא נגישה | לוג + התראה למפעיל; למשתמש: "יש תקלה זמנית, נסה שוב בעוד דקה." |
| הודעה קולית > 3 דקות | "ההקלטה ארוכה מדי — עד 3 דקות." (מגן מעלויות) |
| מכסה חודשית נגמרה | "השתמשת ב-20 ההצעות של חבילת Basic החודש. לשדרוג: <קישור>" |

---

## 7. צינור AI

### 7.1 תמלול
- **קלט:** OGG/Opus מ-WhatsApp, עד 3 דקות.
- **מנוע:** OpenAI Audio API (`whisper-1` / `gpt-4o-transcribe`) כברירת מחדל. הספק והמפתח מוגדרים בדשבורד הסופר-אדמין (§8.6) — לא בקוד. **אחרי הדמו:** בדיקת 20 הקלטות אמיתיות (רעש רקע, מספרים, מונחים) מול Groq Whisper (זול ומהיר), ivrit.ai (מכוונן לעברית), Gemini.
- **prompt לתמלול:** אוצר מילים של התחום + שם העסק, כדי לשפר "נק׳ חשמל", "מ״א", "קומפלט", "ביקור".
- **שני שלבים** (תמלול → מבנה) ולא מודל אודיו ישיר ב-MVP: ניתן לדבג, ניתן להראות למשתמש מה נשמע.

### 7.2 המרה למבנה
**מנוע:** OpenAI (GPT) כברירת מחדל, structured output. ספק ומפתח מהדשבורד (§8.6); Anthropic/Gemini ניתנים להוספה בלי deploy.
**קלט:** תמלול + פרופיל עסק (סטטוס מע״מ, ברירות מחדל) + (שלב 2) קטלוג אישי.
**פלט** (JSON Schema, structured output):

```json
{
  "customerName": "דני כהן",
  "customerPhone": null,
  "title": "התקנת גופי תאורה",
  "items": [
    { "description": "התקנת גוף תאורה", "quantity": 3, "unit": "יח׳", "unitPrice": 150, "priceConfidence": "high" },
    { "description": "ביקור",           "quantity": 1, "unit": "יח׳", "unitPrice": 200, "priceConfidence": "high" }
  ],
  "discount": null,
  "vatIncluded": false,
  "paymentTerms": "50% מקדמה, היתרה בסיום העבודה",
  "validDays": 14,
  "notes": [],
  "needsReview": []
}
```

**כללים:**
- יחידות מותרות: `יח׳`, `מ״ר`, `מ״א`, `שעה`, `יום`, `קומפלט`, `נקודה`. ברירת מחדל `יח׳`.
- "לפני מע״מ" / "פלוס מע״מ" → `vatIncluded: false`. "כולל מע״מ" → `true`. לא נאמר → ברירת המחדל של העסק. עוסק פטור → מע״מ תמיד 0 ו-`vatIncluded` לא רלוונטי.
- "סה״כ 2,000 שקל על הכול" בלי פירוט → פריט אחד `קומפלט`.
- מחיר לא נאמר → `unitPrice: 0`, `priceConfidence: "missing"`, שם הפריט ב-`needsReview`.
- `customerPhone` — אם נאמר מספר. לא חובה ב-MVP (השליחה ידנית).
- שפה: עברית. מספרים בעברית מדוברת ("מאה וחמישים", "אלף וחצי") → מספרים.

### 7.3 תיקונים
**קלט:** JSON נוכחי + הוראה. **פלט:** JSON חדש + `changes[]` (טקסט לתצוגה). כללים זהים ל-7.2.

### 7.4 סיווג הודעות (כשיש טיוטה פעילה)
פלט: `{ intent: "correction" | "new_quote" | "command" | "question" | "unclear", command?: "..." }`.

### 7.5 הערכה
סט קבוע של 30 הקלטות/טקסטים עם JSON צפוי, מורץ על כל שינוי prompt **או ספק**. מדדים: דיוק שם לקוח, דיוק כמויות ומחירים (ה-KPI הקריטי), דיוק מע״מ.

### 7.6 הפשטת ספקים
שני שלבי הצינור הם שתי פונקציות עם ממשק קבוע, וכל ספק הוא מימוש:

```ts
transcribe(audio: Buffer, opts: { language: 'he', hints: string[] }): Promise<{ text: string }>
structureQuote(transcript: string, profile: BusinessProfile): Promise<QuoteJSON>
applyCorrection(quote: QuoteJSON, instruction: string): Promise<{ quote: QuoteJSON, changes: string[] }>
classifyMessage(text: string, hasActiveDraft: boolean): Promise<Intent>
```

מימושים ב-MVP: `openai` לשניהם. הספק הפעיל, המודל והמפתח נקראים מ-`app_settings` (§9) עם cache של 60 שניות; משתני סביבה הם fallback בלבד.

### 7.7 חלופות תמלול — מפת השוק

| אפשרות | מה זה | מסקנה |
|---|---|---|
| **OpenAI** (`whisper-1` / `gpt-4o-transcribe`) | ברירת המחדל. ~2 אג׳/דקה | מתחילים כאן |
| **Groq** (Whisper large-v3) | API מסחרי, free tier נדיב, ~0.7 אג׳/דקה מעבר לו, מהיר מאוד. תואם OpenAI API | המועמד הראשון להחלפה — `custom` URL בדשבורד, בלי קוד |
| **ivrit.ai** | פרויקט ישראלי, Whisper מכוונן לעברית, משקולות פתוחות | כנראה המדויק ביותר לעברית מדוברת ברעש. דורש אירוח (Replicate / HF Inference, אגורות לדקה). **לבדוק על סט ה-eval** |
| Google Speech-to-Text | 60 דקות חינם/חודש | לא — מכסה קטנה, עברית בינונית |
| Whisper self-hosted | חינם | לא — דורש GPU קבוע, סותר serverless |
| Web Speech API | חינם בדפדפן | לא רלוונטי — האודיו מגיע כקובץ מ-WhatsApp |

**עיקרון:** העלות זניחה (§16). מה שמכריע הוא **דיוק בעברית** — נמדד על סט ה-eval (§7.5), לא לפי מחיר.

---

## 8. מסכי Web

כל הדפים: עברית, RTL, mobile-first, נטענים תוך < 2 שניות ב-4G. ללא login.

### 8.1 מסך עריכה — `/e/{token}`
- **גישה:** magic link חתום (HMAC), תוקף 7 ימים, קשור להצעה אחת. "ערוך #1042" בצ׳אט מנפיק קישור חדש.
- **תוכן:** שם לקוח, טלפון (אופציונלי), כותרת, טבלת פריטים (תיאור, כמות, יחידה, מחיר, סה״כ שורה; הוספה/מחיקה/גרירה), הנחה, מע״מ (מחושב, לא ניתן לעריכה ידנית — רק toggle כולל/לפני), תנאי תשלום, תוקף, הערות.
- **פריטים ב-`needsReview`** מודגשים בצהוב.
- **פעולות:** שמור (auto-save), תצוגה מקדימה (= דף הלקוח), הורד PDF, "סמן כנשלחה", מחק.
- **אחרי אישור לקוח:** ההצעה נעולה לעריכה; כפתור "שכפל להצעה חדשה".

### 8.2 דף לקוח — `/q/{publicId}`
- **מזהה:** 8–10 תווים אקראיים. לא ניתן לניחוש.
- **מבנה (מלמעלה למטה):** לוגו + שם העסק + טלפון (לחיץ) · "הצעת מחיר #1042 לדני כהן" · תאריך + תוקף · טבלת פריטים · סה״כ לפני מע״מ / מע״מ / סה״כ · תנאי תשלום · הערות · **כפתור "מאשר את ההצעה"** · "יש לי שאלה" · "לא מתאים לי" (קטן) · "נוצר ב-QuickVoice" (מוסר ב-Pro).
- **אישור:** שם מלא (טקסט, חובה) + חתימה (canvas באצבע) + checkbox "קראתי ומאשר" → נשמר: PNG חתימה, שם, timestamp, IP, User-Agent. הדף עובר למצב "אושר ✅" עם כפתור "הורד PDF חתום".
- **שאלה:** טקסט חופשי → נשלח לבעל המקצוע ב-WhatsApp (6.6). **לא** בונים צ׳אט דו-כיווני — הוא עונה ללקוח ישירות ב-WhatsApp.
- **דחייה:** סיבה אופציונלית (בחירה: יקר / בחרתי אחר / לא רלוונטי / אחר).
- **מעקב צפייה:** אירוע `viewed` ב-GET, דדופ לשעה. צפייה ראשונה → התראה. (צפייה של בעל המקצוע עצמו נספרת — מקובל ב-MVP.)
- **תוקף פג:** הדף מציג "ההצעה פגה — צור קשר עם יוסי חשמל" עם טלפון. בלי כפתור אישור.
- **גרסה:** עד אישור — תמיד המצב הנוכחי. אחרי אישור — snapshot קפוא.

### 8.3 PDF
- אותו HTML של דף הלקוח בתבנית print → Chromium headless (Playwright) → PDF A4.
- שם קובץ: `הצעת-מחיר-1042-דני-כהן.pdf`.
- נוצר לפי דרישה, נשמר ב-R2, URL חתום לשעה ל-`send-doc`.
- גרסה חתומה כוללת: חתימה, שם החותם, תאריך ושעה.

### 8.4 דף הגדרות — `/s/{token}`
שם עסק, לוגו, טלפון, כתובת, ח.פ./ע.מ., עוסק פטור/מורשה, תנאי תשלום ברירת מחדל, הערות קבועות (אחריות, "לא כולל חומרים"), תוקף ברירת מחדל, מונה הצעות התחלתי.

### 8.5 דשבורד
**לא ב-MVP.** רשימת הצעות = פקודת `הצעות` בצ׳אט. דשבורד web (רשימה, סינון, חיפוש, סיכום חודשי) — שלב 2.

### 8.6 דשבורד סופר-אדמין — `/admin`
למפעיל בלבד (אתה). כניסה בסיסמה מ-`ADMIN_PASSWORD` + cookie session; TOTP בשלב 2.

**ספקי AI** — הליבה של הבקשה: להחליף ספק/מודל/מפתח בלי deploy.
| הגדרה | ערכים | הערות |
|---|---|---|
| ספק תמלול | `openai` / `groq` / `custom` (URL תואם OpenAI) | + מודל (`whisper-1`, `gpt-4o-transcribe`, `whisper-large-v3`) |
| מפתח API לתמלול | מוצפן, מוצג ממוסך (`sk-…7f3a`) | כפתור **"בדוק חיבור"** — מתמלל קובץ בדיקה קצר ומציג תוצאה + זמן |
| ספק LLM | `openai` / `anthropic` / `gemini` | + מודל |
| מפתח API ל-LLM | מוצפן, ממוסך | "בדוק חיבור" — מריץ הצעת דוגמה ומציג JSON + זמן + עלות |
| פרומפטים | עריכת system prompt לכל אחת מ-4 הפונקציות (§7.6), עם גרסאות | שלב 2 — לדמו מספיק בקוד |

**iBot** — token, instance_id, webhook token (ממוסכים), סטטוס instance (מחובר/מנותק, מתי נבדק), webhook אחרון שהתקבל, כפתור "שלח הודעת בדיקה אליי".

**משתמשים** — רשימה (טלפון, שם עסק, חבילה, הצעות החודש, הצטרפות, פעילות אחרונה), שינוי חבילה ידני (זה "הסליקה" של ה-MVP), חסימה, איפוס אונבורדינג.

**הצעות** — 50 אחרונות + חיפוש; לכל הצעה: תמלול גולמי, JSON שחזר מהמודל, JSON אחרי עריכה, אירועים, זמן עיבוד ועלות. **זה כלי הדיבוג העיקרי בדמו** — כשבעל מקצוע אומר "זה לא הבין אותי", כאן רואים למה.

**לוג הודעות** — נכנסות/יוצאות, שגיאות עיבוד, כפתור "עבד מחדש" להודעה שנכשלה.

**מונים** — הצעות היום/החודש, עלות AI מצטברת, זמן עיבוד ממוצע (p50/p90), שיעור שגיאות תמלול.

**אבטחת מפתחות:** נשמרים ב-`app_settings` מוצפנים ב-AES-256-GCM עם `SETTINGS_ENCRYPTION_KEY` ממשתני הסביבה. לעולם לא נשלחים ללקוח בשלמותם; לא נכתבים ללוגים.

---

## 9. מודל נתונים

```
users
  id, phone (PK לוגי, מה-jid), display_name (push name), business_name,
  logo_url, business_phone, address, tax_id, vat_status (exempt|registered),
  default_payment_terms, default_notes, default_valid_days (14),
  next_quote_number (1001), plan (trial|basic|pro|unlimited), plan_expires_at,
  onboarding_state (name|vat|logo|done), created_at

quotes
  id, public_id (unique, 10 chars), user_id, number (per user), status,
  customer_name, customer_phone, title,
  vat_included (bool), vat_rate (0.18 | 0), discount_amount,
  payment_terms, notes[], valid_until,
  subtotal, vat_amount, total          -- מחושבים ונשמרים
  transcript, audio_url                -- מקור, לדיבוג
  approved_snapshot (jsonb)            -- קפוא אחרי אישור
  created_at, updated_at, sent_at, first_viewed_at, approved_at, rejected_at

quote_items
  id, quote_id, position, description, quantity, unit, unit_price,
  line_total, needs_review (bool)

quote_events
  id, quote_id, type (created|edited|sent|viewed|approved|rejected|question|pdf|expired),
  payload (jsonb: ip, ua, signature_url, signer_name, reason, question…), at

inbound_messages
  id (msgId, unique), user_phone, type, text, media_url, raw (jsonb), at, processed_at, error

outbound_messages
  id, user_phone, type, body, doc_url, ibot_response, at

app_settings
  key (PK: transcription.provider | transcription.model | transcription.api_key |
       llm.provider | llm.model | llm.api_key | ibot.token | ibot.instance_id |
       ibot.webhook_token | prompt.structure | …),
  value (text; מפתחות — מוצפנים, prefix "enc:"), is_secret (bool), updated_at, updated_by

processing_runs   -- דיבוג ועלות, לכל הודעה קולית
  id, inbound_message_id, quote_id, transcription_provider, transcription_model,
  transcription_ms, transcript, llm_provider, llm_model, llm_ms, llm_input_tokens,
  llm_output_tokens, raw_llm_output (jsonb), cost_estimate, error, at

catalog_items   -- שלב 2
  id, user_id, description_normalized, unit, last_price, avg_price, times_used, last_used_at
```

---

## 10. סטטוסי הצעה

```
draft ──"שלח"/"שלחתי"/כפתור──▶ sent ──צפייה──▶ viewed ──▶ approved
  │                              │                │   └──▶ rejected
  │                              └── צפייה ישירה מ-draft → viewed (מדלג על sent)
  └──"בטל"──▶ (נמחקת)
                              כל סטטוס לא סופי ──תוקף──▶ expired
```

- `sent` הוא הצהרתי (אנחנו לא רואים את ה-Forward). צפיית לקוח מקדמת ל-`viewed` מכל מצב קודם.
- `approved` / `rejected` סופיים. אחרי `rejected` אפשר "שכפל" לטיוטה חדשה.
- `paid` — שלב 2 (סליקה).

---

## 11. תמחור ומגבלות

| חבילה | מחיר | הצעות/חודש | כולל |
|---|---|---|---|
| ניסיון | 0 | 5 (סה״כ, לא חודשי) | הכול |
| Basic | 49 ₪ | 20 | לוגו, PDF, אישור לקוח |
| Pro | 99 ₪ | 100 | + חתימה דיגיטלית, הסרת "נוצר ב-QuickVoice", תזכורות, דשבורד |
| Unlimited | 149 ₪ | ∞ | + חיבור WhatsApp אישי (שלב 2), סליקת מקדמות |

**שינוי מ-start.md:** לוגו עובר ל-Basic. הלוגו הוא מה שהופך את ההצעה ל"מקצועית" — זה ערך הליבה, לא upsell. ב-Pro נועלים חתימה, מיתוג ותזכורות.

- **ספירה:** הצעות שנוצרו (לא נשלחו) בחודש קלנדרי. טיוטה שבוטלה תוך 5 דקות לא נספרת.
- **מכסה נגמרה:** הבוט עדיין מתמלל ומציג סיכום, אבל לא יוצר קישור — "לשדרוג: <קישור>".
- **תשלום:** ב-MVP ידני (העברה/ביט + הפעלה ידנית). סליקה בשלב 2.

---

## 12. תיחום

### MVP (דמו ל-10 בעלי מקצוע) — חייב לעבוד מקצה לקצה
1. Webhook + נורמליזציה + סינון + דדופ (§5.1)
2. אונבורדינג בצ׳אט (§6.2)
3. הודעה קולית → תמלול → JSON → סיכום + קישורים (§6.3)
4. תיקונים בצ׳אט (§6.4) ופקודות בסיס: `הצעות`, `שלח`, `PDF`, `בטל`, `עזרה`
5. מסך עריכה (§8.1)
6. דף לקוח עם אישור + חתימה + שאלה + דחייה (§8.2)
7. ~~PDF לפי דרישה (§8.3)~~ — נדחה, ראה §16
8. התראות: צפייה, אישור, דחייה, שאלה (§6.6)
9. דף הגדרות (§8.4)
10. ניטור בריאות instance (§5.5)
11. דשבורד סופר-אדמין (§8.6): ספקי AI + מפתחות, iBot, משתמשים, הצעות עם דיבוג, לוג הודעות

### שלב 2 (אחרי שיש 10 משתמשים פעילים)
- קטלוג אישי / זיכרון מחירים — השלמת מחירים אוטומטית
- תזכורות ופקיעת תוקף
- דשבורד web + דוח שבועי בצ׳אט
- סליקת מקדמות (Grow / PayPlus / Tranzila)
- תמונה של ההצעה (`send-image`) לתצוגה מקדימה בצ׳אט
- חיבור WhatsApp אישי של בעל המקצוע כ-instance ב-iBot: שליחה ללקוח מהמספר שלו, תזכורות ללקוח, מעקב אוטומטי — **הבידול שרק QuickVoice יכול להציע בזול**
- ייצוא לחשבונית (Green Invoice / מורנינג / iCount)
- ערבית ורוסית

### לא בונים (במפורש)
- חשבוניות / קבלות / הנה"ח
- CRM, יומן, מלאי, ניהול עובדים
- אפליקציה native / PWA (WhatsApp הוא האפליקציה)
- צ׳אט לקוח–בעל מקצוע בתוך המערכת
- WhatsApp Cloud API של Meta (עד שיש סיבה)

---

## 13. מדדים

| מדד | יעד לדמו |
|---|---|
| זמן מהודעה קולית לסיכום בצ׳אט | < 30 שניות (p90) |
| דיוק כמויות ומחירים ללא תיקון | > 85% מההצעות |
| הצעות לשבוע למשתמש פעיל | ≥ 3 |
| משתמשים שיצרו הצעה שלישית | ≥ 6 מתוך 10 |
| אחוז אישור לקוח (מהצעות שנצפו) | נמדד, לא יעד |
| מוכנות לשלם 49 ₪ | ≥ 4 מתוך 10 — נבדק בשיחה |

---

## 14. סיכונים

| סיכון | חומרה | מענה |
|---|---|---|
| ניתוק instance של iBot — הודעות נעלמות בשקט | גבוהה | §5.5: ניטור + התראה; הודעת שגיאה למשתמש כשחוזר |
| חסימת המספר ע"י WhatsApp (חיבור לא רשמי) | גבוהה | מספר ייעודי; נפח נמוך; רק תשובות להודעות נכנסות (לא שיווק); שכבה ניתנת להחלפה ל-Cloud API |
| מדיה ב-iBot ציבורית ונמחקת אחרי 30 יום | בינונית | §5.2: משיכה מיידית ל-R2, לא שומרים URL |
| איכות תמלול עברית ברעש | בינונית | בדיקת 20 הקלטות לפני בחירת מנוע; fallback לטקסט; "לא הצלחתי לשמוע" |
| LLM ממציא מחיר שלא נאמר | גבוהה (אמון) | `priceConfidence` + `needsReview`; מחיר חסר = 0 מודגש, לעולם לא ניחוש |
| בעל המקצוע מעביר לפני שתיקן | נמוכה | הקישור חי עד אישור (§6.3) |
| עלות AI לכל הצעה | נמוכה | ~0.05–0.15 ₪ להצעה; מגבלת 3 דקות; מכסות חבילה |

---

## 15. החלטות פתוחות

| # | שאלה | המלצה | סטטוס |
|---|---|---|---|
| 1 | `mediaUrl` בפיילוד | ✅ נוסף (20.9) | סגור |
| 2 | הסרת `userData` + header token | ✅ תוקן (20.9) | סגור |
| 3 | endpoint סטטוס instance / webhook על ניתוק | `GET /instance-status` | ממתין ל-iBot |
| 4 | לוגו ב-Basic | כן | לאישור |
| 5 | מנוע תמלול | Whisper, אחרי בדיקת 20 הקלטות | לבדיקה |
| 6 | דומיין קצר לקישורים | נדרש (קישור ב-WhatsApp צריך להיות קצר וברור) | לבחירה |
| 7 | האם לשלוח ללקוח ישירות מהמספר של QuickVoice אם ניתן טלפון? | לא ב-MVP (מספר זר = פחות אמון) | סגור |

---

## 16. טכנולוגיה

**עיקרון:** הכול serverless על Vercel — אין שרת שרץ כל הזמן, אין מודל AI שמתארח אצלנו. תמלול ו-LLM הם קריאות API.

| רכיב | בחירה | הערות |
|---|---|---|
| Web/API | Next.js 16 (App Router, Route Handlers), TypeScript, Vercel | |
| DB | Neon Postgres + Drizzle (`@neondatabase/serverless`) | |
| תמלול | OpenAI Audio API (`whisper-1` / `gpt-4o-transcribe`) | מקבל OGG/Opus ישירות. ~2 אג׳/דקה. ספק/מודל/מפתח מהאדמין (§8.6) |
| LLM (JSON, תיקונים, סיווג) | OpenAI GPT, structured output | ספק/מודל/מפתח מהאדמין. Anthropic/Gemini כמימושים נוספים של §7.6 כשיהיה eval |
| הגדרות ומפתחות | `app_settings` ב-Neon, מוצפן AES-GCM | env = fallback בלבד |
| עיבוד webhook | `waitUntil` מ-`@vercel/functions` — 200 מיד, עיבוד ממשיך ברקע | כתיבה ל-`inbound_messages` לפני ה-200; cron מרים תקועים. Inngest/QStash כשיש משתמשים אמיתיים |
| Cron | דמו: cron-job.org / UptimeRobot → endpoint. Pro: Vercel Cron | Hobby מוגבל לפעם ביום |
| קבצים | Vercel Blob (או R2) — הקלטות 30 יום, לוגואים, חתימות, PDF | |
| PDF | **נדחה מהדמו.** אח"כ: שירות HTML→PDF חיצוני (Browserless/PDFShift) או `@sparticuz/chromium` | פונטים בעברית + RTL לבדוק |
| WhatsApp | iBot — מאחורי `lib/whatsapp/` עם ממשק אחד (`sendText`, `sendDoc`, `parseInbound`) | |
| ניטור | Sentry + התראת instance-down | |

**עלות משתנה להצעה:** ~7 אגורות (תמלול + LLM). זניח מול 49 ₪/חודש.

**זמן עיבוד צפוי:** הורדה 0.5 שנ׳ + תמלול 3–8 שנ׳ + LLM 2–5 שנ׳ + שליחה 1 שנ׳ ≈ **7–15 שניות** מהודעה קולית לסיכום בצ׳אט.
